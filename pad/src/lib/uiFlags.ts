/**
 * Student-first UI for EquaNote.
 * When true, rare/power tools stay in the codebase but are hidden from the default surface.
 * Flip to false to restore Classes, templates, MathML copy, JSON backup, and the rest.
 */
export const STUDENT_SIMPLE_UI = true;
